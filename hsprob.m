function probability = hsprob(deckSize, turnNo, cardNo, testNo, coin)
    
    %HSPROB  Computes liklihood of drawing a desired card given arbitrary
    %input arguments, currently in prototype phase
    
    probability = 1;
    varType = class(deckSize);
    fprintf("%s\n", varType);
    
    %check args are all present
    
    
    %check that args are of type double
    
    %apply relevant arg values
    
    %perform computation
    %Mulligan
    mulliganDoubleArray = double.empty(4,0);
    disp(mulliganDoubleArray);
    
    %Reset Mulligan
    %Draw Probability
    
    
    
end